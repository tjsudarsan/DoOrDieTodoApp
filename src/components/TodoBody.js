import React from "react";
import TodoForm from "./TodoForm";

const TodoBody = () => {
    return (
        <React.Fragment>
            <TodoForm />
        </React.Fragment>
    );
};

export default TodoBody;
